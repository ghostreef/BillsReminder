<!DOCTYPE html>
<html>
<head>
  <title>BillsReminder</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href='https://fonts.googleapis.com/css?family=Source+Sans+Pro:400,600,300' rel='stylesheet' type='text/css'>
  <link href="http://fonts.googleapis.com/css?family=Oswald:400,300" rel="stylesheet" type="text/css">
</head>
<body
<% if content_for?(:body_attributes) %>
<%= yield(:body_attributes) %>
<% end %>>

  <% if Parser.parser.nil? %>
    <div class='flash-error'>There is no enabled parser!</div>
  <% end %>

  <!-- move to before action later -->
  <%= content_tag(:div, 'A good place to start is by creating some purposes.', class: 'flash-notice') if Purpose.count == 0 %>
  <%= content_tag(:div, 'Sources show where a transaction originated from, add some under the sources tab.', class: 'flash-notice') if Source.count == 0 %>
  <%= content_tag(:div, "Looks like you're ready to begin uploading some transactions.", class: 'flash-notice') if Source.count != 0 &&  Purpose.count != 0 %>
  <%= content_tag(:div, 'Categories help you group your transactions by purpose(s) or source(s) and are useful to see trends in spending. Create some categories under the categories tab.', class: 'flash-notice') if Source.count != 0 &&  Purpose.count != 0 %>
  <%= content_tag(:div, 'Categories are not always mutually exclusive. Use sets to group your categories into sets.', class: 'flash-notice') if CategorySet.count == 0 && Category.count != 0 %>

  <div class="outer-container">
    <%= render 'header' %>
  </div>

  <%= render 'flash_messages' %>

  <div class="outer-container">
    <div class="outer-container main">
      <%= yield %>
    </div>
  </div>

</body>
</html>
